services:
  mongodb:
    image: mongo:7
    container_name: recommendation-mongodb
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: aibert_recommendation
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
